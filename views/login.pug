extends layouts/main

block content
    form(action=routes.login, method="post")
        input(type="text", name="name", placeholder="Put your id in") 
        input(type="password", name="password", placeholder="Put your password in") 
        input(type="submit", value="Login")
    include partials/socialLogin